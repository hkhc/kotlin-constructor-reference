plugins {
    // *** The test code no problem with 1.3.72
    // *** IntelliJ 2020.2 with Kotlin 1.4.10 plugin show error with constructor reference in
    //     Main.kt
    // *** But it is fine to run the code directly without intellij
    id 'org.jetbrains.kotlin.jvm' version '1.3.72'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

compileKotlin {
    kotlinOptions {
        freeCompilerArgs += "-XXLanguage:+NewInference"
    }
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib"
}
